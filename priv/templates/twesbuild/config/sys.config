[
    {arizona, [
        {server, #{
            enabled => true,
            routes => [
                {asset, ~"/assets/js/arizona-worker.min.js",
                 {priv_file, arizona, ~"static/assets/js/arizona-worker.min.js"}, []},
                {asset, ~"/favicon.ico", {priv_file, twesbuild, ~"static/favicon.ico"}, []},
                {asset, ~"/robots.txt", {priv_file, twesbuild, ~"static/robots.txt"}, []},
                {asset, ~"/assets", {priv_dir, twesbuild, ~"static/assets"}, []},
                {asset, ~"/images", {priv_dir, twesbuild, ~"static/images"}, []},
                {view, ~"/", twesbuild_view, #{title => ~"Arizona Framework"}, []},
                {websocket, ~"/live", #{}, []}
            ]
        }},
        {reloader, #{
            enabled => true,
            rules => [
                #{
                    handler => {twesbuild_css_reloader, #{}},
                    watcher => #{
                        directories => ["assets/css"],
                        patterns => [".*\\.css$"]
                    }
                }
            ]
        }},
        {plugins, [
            {arizona_rebar3_plugin, #{}}
        ]}
    ]}
].
